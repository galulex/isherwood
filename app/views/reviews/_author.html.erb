<div class="container review-show-padding" id="sidenav">
  <div class="row">
  
  </div>
  <div class="col-xs-12 col-md-8 col-md-offset-2">
    <%= render "content" %>
    <br>
    <br>
    <hr>
    </div>
</div>

<div class="container" id="photos">
  <% @review.photos.each do |photo| %>
    <div class="col-xs-12 col-md-8 col-md-offset-2">
    <%= image_tag photo.file_name.url, :class => "img-responsive" %>
    </div>
  <% end %>
</div>

<div class="comments-bg" id="comments">
  <div class= "container"> 
  <div class= "col-md-8 col-md-offset-2">
 <div class="review_footer">
  <br>
  <%= link_to @review.brand.label, @review.brand, :class => 'btn btn-sm btn-lake' %>
  <br> <br>
  <%= render_like_conditionally %>

  <span class="votes-count" data-id="<%= @review.id %>">
  <%= @review.get_likes.size %>
  </span>

    <h1><%= render_comments_count %></h1>
    <% @comments.each do |comment| %>
     <div id="comment_<%= comment.id %>">
       <hr>
      <p class="comment-user"><strong><%= comment.user.username.upcase %></strong></p><p class="timeago">&nbsp&nbsp&nbsp<%="#{time_ago_in_words(comment.created_at)} ago"%></p>
      <br>
      <br>
      <p><%= comment.comment %><p>
       <%= link_to [@review, comment], method: :delete, remote: true do %>
          <i class="glyphicon glyphicon-remove"></i>
       <% end %>
       </div>
    <% end %>
  </div>
  <%= form_for([@review, @review.comments.build]) do |f| %> 
<div class="form-group">
    <%= f.text_area :comment, :class => 'form-control', data: { behavior: 'autocomplete' } %>
</div> 

  <%= f.submit 'Submit', :class => 'btn btn-sm btn-main' %> 
  <a id="last-comment"></a>
<% end %> 
  </div>
</div>
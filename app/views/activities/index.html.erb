<% @activities.each do |activity| %>
  <% if activity.trackable %>
    <% if activity.trackable.commentable.user == current_user %>
      <div class="activity">
      <%= link_to activity.owner.username, activity.owner if activity.owner %>
      <%= render_activity activity %>
      </div>
    <% end %>
  <% end %>
<% end %>
<nav class="navbar navbar-basis navbar-fixed-top">
    <div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
		<a class="navbar-brand" href="<%= root_url %>"><img src="" alt="Workspace">
        </a>
		</div>
		<div class="collapse navbar-collapse" id="navbar">
			<ul class="nav navbar-nav navbar-left">
		    <li><%= link_to "Explore", explore_index_url %></li>
			</ul>
		<ul class="nav navbar-nav navbar-right">
			 <% if user_signed_in? %>
			  <li> <%= link_to "Write a review", new_review_path %></li>
			  <li><%= link_to activities_path do %>
			    <i class="glyphicon glyphicon-bell"></i>
			  <% end %></li>
  			<li class="dropdown">
  		    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= image_tag current_user.avatar, :class => "card-avatar" %></a>
  			<ul class="dropdown-menu" role="menu">
		 	<li><%= link_to "My Profile", user_path(current_user) %></li>
		    <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
		 	<li><%= link_to "Settings", edit_user_registration_path %></li>
			</li>
		    </ul>
			<% else %>
			<li class="active"><%= link_to "Sign Up", new_user_registration_path %><span class="sr-only">(current)</span></a></li>
			<li class="active"><%= link_to "Log In", new_user_session_path %><span class="sr-only">(current)</span></a></li>
			<% end %>
		</ul>
		<%= form_for :term, html: {class: "navbar-form navbar-left search-form"}, url: search_path, method: :get do |form| %>
    		<%= text_field_tag :term, params[:term], class: 'form-control', placeholder: "Search" %>
		<% end %>
		</div>
	</div>
</nav>
I"�(function() {
  var options;

  $(“.brand - autocomplete”).selectize(options);

  options = {
    maxOptions: 3,
    render: {
      item: function(item, escape) {
        return "<div class='brand-item'><span>" + (escape(item.brand)) + "</span></div>";
      },
      option: function(item, escape) {
        return "<div class='brand-item'><span>" + (escape(item.brand)) + "</span></div>";
      }
    },
    valueField: ‘brand’,
    searchField: ‘brand’,
    create: true
  };

}).call(this);
:ET